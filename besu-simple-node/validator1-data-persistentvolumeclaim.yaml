apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: validator1-data
  name: validator1-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
